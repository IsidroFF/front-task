/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        API_URI: 'http://localhost:8080',
        //API_URI: 'https://backend-electron.onrender.com',
    }
};

export default nextConfig;
